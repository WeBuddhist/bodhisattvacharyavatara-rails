Read `4-SYSTEM/Skills/tag-inline-toc/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Insert markdown headings with ^N-N-0 block IDs at section boundaries in a formatted Tibetan commentary, then wrap the inline structural announcement phrases (sa bcad) with Obsidian wikilinks per CLAUDE.md §5b.
